import React from 'react';

const Logo = () => {
    return (
        <div className={'logo'}>
            <a href={'/'}>
                <img src={'/images/logo.svg'} alt={'Logo'} />
            </a>
        </div>
    )
}

export default Logo;